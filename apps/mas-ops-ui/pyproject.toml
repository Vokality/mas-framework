[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mas-ops-ui"
version = "0.4.5"
description = "MSP ops-plane UI workspace"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
