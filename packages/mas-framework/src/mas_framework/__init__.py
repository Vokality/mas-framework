"""mas-framework meta-package.

This package has no code of its own. It exists only to pull in the
component packages via dependencies:

- mas-proto
- mas-core
- mas-gateway
- mas-server
- mas-agent

Install with: pip install mas-framework
"""
